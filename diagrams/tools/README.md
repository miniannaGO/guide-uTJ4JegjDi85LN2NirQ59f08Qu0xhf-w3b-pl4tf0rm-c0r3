# Herramientas

Estos diagramas explican cómo los comandos `composer robo` se conectan con `tools/`, `robo-configurations.json`, `app-gateway` y `app-surface`.

## Índice

1. [Robo, scaffolding y contratos](01-robo-tools-commands.md)

## Lectura rápida

Usa este bloque cuando necesites saber qué comando crea un artefacto y dónde termina la automatización. Si una regla del diagrama contradice `app-gateway/RoboFile.php`, `robo-configurations.json` o los generadores bajo `tools/`, manda el código.
